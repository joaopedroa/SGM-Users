server.servlet.context-path=/api/v1

spring.application.name=sgm-users-service
server.port=${PORT:0}
#security.user.password= ${eureka.password}
# Eureka
#eureka.client.service-url.defaultZone=${EUREKA_URL:http://user:password@localhost:8761}/eureka/
eureka.client.service-url.defaultZone=https://sgm-eureka-server.herokuapp.com/

# Porta aleatória
eureka.instance.instance-id=${spring.application.name}:${spring.application.instance_id:${random.value}}